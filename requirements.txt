flask
flask-wtf
reportlab
openpyxl
qrcode[pil]
pdfplumber
gunicorn

# OCR for scanned/photographed tickets. PyMuPDF renders the pages and needs no
# system packages; pytesseract is a wrapper and also needs the tesseract-ocr
# binary installed (see README). Uploads fall back to manual entry without it.
pytesseract
PyMuPDF
airportsdata

# Postgres-backed booking tracker. Without DATABASE_URL the app never imports
# these and records to the spreadsheet instead, so they are only needed on a
# host whose filesystem does not survive a restart.
psycopg[binary]
psycopg-pool
